[package]
name = "blacknode-dataset"
version = "0.3.6"
description = "Blacknode-native episode recording, lazy dataset adapters, validation, HDF5, Hugging Face Hub and LeRobot interoperability."
requires-blacknode = ">=0.1.0"
layer = "learning"
component-mode = true

[categories]
"Dataset" = "#8b5cf6"

[dependencies]
pip = ["numpy>=1.24"]
imports = ["numpy"]

[components.adapters]
description = "Lazy BlacknodeDataset views and external dataset source adapters."
default = false
capabilities = ["dataset.adapters", "dataset.lerobot"]
node-types = ["LeRobotDataset"]

[components.adapters.dependencies]
pip = ["pyarrow>=16"]
imports = ["pyarrow"]

# Roadmap component; sources land in components/recording/nodes.
[components.recording]
description = "Episode capture, dataset creation, and stream browsing."
default = true
capabilities = ["dataset.recording"]
templates = ["components/recording/templates"]
node-types = [
  "DatasetBrowser", "DatasetCameraStreamList", "DatasetCreate", "EpisodeRecorder"
]

[components.recording.dependencies]
pip = ["pyarrow>=16", "opencv-python>=4.8", "imageio-ffmpeg>=0.5"]
imports = ["pyarrow", "cv2", "imageio_ffmpeg"]

# Roadmap component; sources land in components/replay/nodes.
[components.replay]
description = "Episode replay and trajectory smoothing."
default = true
capabilities = ["dataset.replay"]
node-types = [
  "EpisodeReplay", "TrajectorySmoother"
]

[components.replay.dependencies]
pip = ["pyarrow>=16", "opencv-python>=4.8"]
imports = ["pyarrow", "cv2"]

# Roadmap component; sources land in components/validation/nodes.
[components.validation]
description = "Dataset validation, summaries, and statistics."
default = true
capabilities = ["dataset.validation"]
node-types = [
  "EpisodeDatasetSummary", "EpisodeDatasetValidate", "EpisodeStats"
]

[components.validation.dependencies]
pip = ["pyarrow>=16"]
imports = ["pyarrow"]

# Roadmap component; sources land in components/evaluation/nodes.
[components.evaluation]
description = "Episode scoring and evaluation."
default = false
capabilities = ["dataset.evaluation"]
node-types = [
  "EpisodeEvaluator"
]

[components.evaluation.dependencies]
pip = ["pyarrow>=16"]
imports = ["pyarrow"]

# Roadmap component; sources land in components/export/nodes.
[components.export]
description = "HDF5 and LeRobot dataset export."
default = false
capabilities = ["dataset.export"]
node-types = [
  "HDF5EpisodeExport", "LeRobotV3Export"
]

[components.export.dependencies]
pip = ["pyarrow>=16", "pandas>=2.0", "opencv-python>=4.8", "h5py>=3.11", "imageio-ffmpeg>=0.5"]
imports = ["pyarrow", "pandas", "cv2", "h5py", "imageio_ffmpeg"]

# Roadmap component; sources land in components/publishing/nodes.
[components.publishing]
description = "Hub upload and repository publishing."
default = false
capabilities = ["dataset.publishing"]
node-types = [
  "BlacknodeHubExport", "HuggingFaceDatasetUpload", "StreamPublisher"
]

[components.publishing.dependencies]
pip = ["huggingface-hub>=0.24"]
imports = ["huggingface_hub"]
requires = [{ component = "export" }]
